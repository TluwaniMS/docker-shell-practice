const name = process.env.NAME || 'John'
const surname = process.env.SURNAME || 'Doe'

const message = `Greetings to you ${name} ${surname}, and welcome to the chocolate factory!!!`;

console.log(message);
